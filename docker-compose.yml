services:
  database:
    image: postgres:18.6-trixie
    container_name: referentiel-database
    network_mode: 'host'
    environment:
      POSTGRES_PASSWORD: password
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready']
      interval: 1s
      timeout: 5s
      retries: 10
