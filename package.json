{
  "name": "referentiel-national-agrements",
  "version": "0.0.0",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "test": "mocha tests/**",
    "lint": "npm run lint:filenames && npm run lint:files",
    "lint:files": "oxfmt --check",
    "lint:files:fix": "oxfmt",
    "lint:filenames": "ls-lint"
  },
  "dependencies": {
    "@ls-lint/ls-lint": "^2.3.1"
  },
  "devDependencies": {
    "chai": "^6.2.2",
    "mocha": "^12.0.0",
    "oxfmt": "^0.67.0"
  },
  "engines": {
    "node": "^26.8.1"
  }
}
